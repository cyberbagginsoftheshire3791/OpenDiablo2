# Bug database and launch ledger

The error log Constitution I.3 and Plan §7 require, and the home Bethke's *"bugs
without owners simply will not be addressed"* asks for. Every known defect gets a
row: **id · first seen · build · reproduction · rate · owner · status.** A bug
without an owner is not logged. New defects are added here the day they are found,
not at some future audit.

Seeded 14 September 2026 by the hardening burst with the five defects that were
living as prose under "Parked" in `state.md`. The three map-load faces are parked
together on Josh's ruling (28 Aug): `MapEngine.asset` is a concrete type, so there
is no seam to inject a failing loader, and a nil guard would make the game *quiet
rather than correct* — **do not "just add the return"**; they go into the DT1
milestone with the loader-interface extraction that makes them testable. All are
intermittent: **re-run before investigating.**

Added 17 September 2026 (Cowork): **BUG-8 and BUG-9**, the two sight defects. An
independent Codex audit of `72bc622c` found the `checkLos` endpoint overshoot; a
verification pass reproduced it with a line-for-line port and measured its incidence
over the signed notice radius. BUG-9 was visible in the d2-formats reference as an
as-found observation and had never been given a row. **Both were given owners by Josh the same day:**
BUG-8 to **the sight burst**, a burst of its own ruled to run BEFORE M4.4c-2a --
because c-2a exists to measure pacing, and an awareness ray that is wrong on 57%
of casts would have had the first real numbers measured through a broken sensor.
BUG-9 to the DT1 milestone. **Neither is fixed by this commit** -- neither is in
c-2's signed scope, which is why BUG-8 got a burst rather than a place in it.

Added 19 September 2026 (Cowork), after Josh launched the game for the first time
and could do nothing but walk around and die: **BUG-10 and BUG-11**, and BUG-9
shipped. The three are one story told from three sides -- the night went quiet
because sight refused rays at eight tiles (BUG-9), because a beaten pack held a
slot in the group cap until sunrise (BUG-11), and because a routed beast is
unwatched forever and stands where it broke (BUG-10, the half still open). **All
three were invisible to every instrument this project owns**, and the reason is
the same one the 19 September audit is about: every test tested a part. See
`The Sight Burst - S0 Measurements.md` for the evidence and
`playtest/unaided_test.go` for the gate that was missing.

## Defects

| id | first seen | build | reproduction | rate | owner | status |
|---|---|---|---|---|---|---|
| BUG-1 | pre-fork (upstream) | any | `MapEngine.addDT1` logs a failed `LoadDT1` then dereferences `dt1.Tiles` — nil panic on a DT1 that will not load (`d2core/d2map/d2mapengine/engine.go:102-107`) | intermittent (map load) | the DT1 milestone | PARKED |
| BUG-2 | pre-fork (upstream) | any | `d2dt1.DecodeTileGfxData` indexes `(*pixels)[offset]` with no bound — index-out-of-range on malformed tile gfx (`d2common/d2fileformats/d2dt1/gfx_decode.go:30,60`) | intermittent (map load) | the DT1 milestone | PARKED |
| BUG-3 | pre-fork (upstream) | any | `MainMenu.createLogos` logs then dereferences a nil logo (`d2game/d2gamescreen/main_menu.go:325-331`) | intermittent (startup) | the DT1 milestone | PARKED |
| BUG-4 | ~Aug 2026 | any | the black floor: cached floor tiles are provably fully coloured while the composite draws nothing. A NIGHT screenshot is useless as evidence — the night scripts sample daylight control frames for exactly this reason | unmeasured, per launch | unassigned | PARKED |
| BUG-6 | 2026-09-15 | M4.4c-1 | a squad model that dies keeps its squad's overhead bar: `Squads.Bars` lists every member entity with `entity != ""` regardless of `health` (`d2core/d2world/squads.go`), and the corpse guard added to `Game.OverheadBars` only covers the ENEMY loop. **It cannot happen in friends build #1** -- `s:1`'s one model is the player, and a dead player is the death screen -- so the fix waits for the milestone that ships a second squad in a shipped build | not reproducible in build #1 (needs N>1) | M4.4c-2 | OPEN |
| BUG-7 | 2026-09-15 | M4.4c-1 | **no playtest can exercise a HELD mouse button.** `strigoi_click` presses and releases inside one frame (`d2app/harness_input.go`), so `repeatDue(now, now)` is false by construction and `GameControls.OnMouseButtonRepeat` is unreachable from any script. The squad guard added there on 15 Sep is verified by READING only. A harness verb that holds a button for N frames would close it | always (instrument gap) | M4.4c-2 | OPEN |
| BUG-5 | ~Aug 2026 | any | the startup / map-load crash — presents as one of BUG-1/2/3. Intermittent; **re-run before investigating**. Every playtest launch is counted in the ledger below so the rate can be measured before anything ships (9 Sep G9; the "before friends build #1" anchor retired 17 Sep 2026) | intermittent | unassigned | PARKED |
| BUG-8 | 2026-09-17 | `72bc622c` | **line of sight samples one subtile PAST its destination.** `MapEngine.checkLos` loops `for i := 0; i <= int(N); i++` and increments the coordinates BEFORE sampling (`d2core/d2map/d2mapengine/pathfind.go:124-136`), so it takes `int(N)+1` steps along a segment `N` long -- a full extra subtile whenever `N` is whole, which is every axis-aligned and 45 deg ray between grid-aligned entities. A ray (2,2)->(4,2) samples (3,2), (4,2), **(5,2)**: a blocker beyond the target blocks it, while the reverse ray is clear, and a legal map-edge endpoint is rejected because `SubTileAt` returns nil off-map and `:134` treats nil as a wall. **Reachable in live gameplay, not just pathfinding:** `d2game/d2gamescreen/game.go:839-850` (`mapSight.Clear`) is the only `Sight` implementation and `d2core/d2world/notice.go:341` gates awareness on it **one-directionally**, so the forward/reverse asymmetry never averages out -- cover works or does not depending on which side of the player the watcher stands. It also contaminates evidence: `d2app/harness_obs.go:559` uses it for `StraightLineClear`, the negative control that proves a route was a genuine detour. **The fix is NOT simply `<=` -> `<`:** with `<` the loop takes `int(N)` steps, which lands exactly on the endpoint only when `N` is WHOLE -- for a FRACTIONAL `N` (any ray between entities that are not grid-aligned) the last sample falls short and the destination cell is never examined, so a watcher sees through a blocker standing on the player. It trades this false-negative for a false-positive on a different set of rays. Use `ceil(N)` steps with the final step clamped to the endpoint (`t := min(float64(i), N)`), keep not sampling the start cell, and assert WHICH cells the ray samples plus forward/reverse symmetry -- no existing test does (`accessors_test.go:118-146` asserts only `NotPanics` and loose bounds, and its one clear case passes by luck; `d2world/notice_test.go:40-45` uses a `fakeSight` and never runs the real cast). **Re-measure real-map awareness before retuning spawn aggression -- failed awareness resembles a pacing problem.** | **57.4%** of 200,000 simulated awareness rays inside the signed 12-tile radius inspect at least one subtile the segment never reaches (port; live-map incidence unmeasured) | **THE SIGHT BURST** -- ruled by Josh 17 Sep 2026 to run BEFORE M4.4c-2a, so c-2a's first pace numbers come through a working sensor | **FIXED 17 Sep 2026** -- `ceil(N)` steps with the final step clamped to the endpoint (`travelled := math.Min(float64(i), N)`), the position computed from `start` each step so a long ray does not drift. Five tests in a new `d2core/d2map/d2mapengine/los_test.go`: destination-not-overshot, forward/reverse symmetry (integer AND subtile-centre), destination-still-sampled (whole AND fractional N -- the `<` trap), map-edge endpoint (whole AND fractional), and the no-regression pair (a blocker between still blocks, the start cell is still not sampled). **Three negative controls run in place:** the original loop -> 4 red · `steps := int(N)` -> the fractional-destination test alone goes red · no clamp -> the fractional map-edge test alone goes red. Gate green (gofmt, both builds, vet x3, 39 ok / 0 fail, 0 ebiten in the four leaf packages, fixtures, mod tidy); all 25 playtest scripts green over 20 launches. **One behaviour change, pinned rather than left to accident:** a zero-length ray from inside a blocker now reads clear, because the loop no longer runs for `N == 0` |
| BUG-9 | 2026-09-17 | `72bc622c` (pre-fork behaviour) | **the sight test consults the WALK flag.** `checkLos` blocks on `flags.BlockWalk` (`pathfind.go:134`); `BlockLOS` is decoded and never read anywhere in the engine, recorded verbatim at `.claude/skills/d2-formats/references/maps.md:184` (*"Only `BlockWalk` is consulted anywhere in the engine; `BlockLOS`, `BlockJump`, `BlockPlayerWalk` and `BlockLight` are decoded and never read"*); `docs/architecture-as-found.md:140` states only the narrower *"walkability is one bit"*. **Re-verified 17 Sep:** `git grep -n BlockLOS -- '*.go'` returns only `d2common/d2fileformats/d2dt1/subtile.go` and its unit test. Anything walkable-but-opaque, or blocking-but-see-through, is therefore wrong for sight. **Distinct from BUG-8 and must not be folded into it:** fixing the endpoint does not make a sight test read the sight flag, and reading the sight flag does not fix the endpoint. Described in the as-found docs since the hardening burst; never given a row until now | **MEASURED 19 Sep 2026, and the answer inverts the remedy: ZERO subtiles disagree in the direction that matters.** `go run ./tools/subtilecensus` over every DT1 of Act 1 Town, Wilderness and Cave (38,425 subtiles): **BlockWalk 10,252 (26.68%), BlockLOS 1,194 (3.11%), both bits 1,194, BlockLOS-ONLY 0, BlockWalk-ONLY 9,058.** `BlockLOS` is a **strict subset** of `BlockWalk` in this art, so swapping the word can never ADD a blocker -- it only removes them, cutting sight blockers from 10,252 to 1,194, an **88.4% reduction** | **re-owned 19 Sep 2026 from the DT1 milestone to the sight burst**, because the census made it a difficulty question rather than a tile-format one, and because BUG-10 made it the defect standing between the build and a night that works. Josh's stated precondition -- answer the data question first -- was correct and was met before anything shipped | **FIXED 19 Sep 2026, AND THE FILED REMEDY WAS WRONG BEFORE IT WAS RIGHT.** The defect is real: a bit D2 authored is decoded and read nowhere. But *"read `BlockLOS` instead"* is **not a fix** -- with BlockLOS-only = 0 it is a pure removal of 88.4% of sight blocking, a sweeping difficulty change wearing a bug fix's clothes. **An A/B on the unaided chain measured what it does** (same seed, baseline reproduced to 13 decimal places; `strigoi-harness-runs\ab-A-baseline.txt` / `ab-B-blocklos.txt`): first awareness 19:56 -> **03:00**, peak hunters aware 3 -> **17**, first fight 20:27 -> **03:31**, player **dead at 04:32** having survived the night in the baseline. That is unblocked sight, not corrected sight. **Three untagged tests also fail under the swap** -- `TestPathFindRoutesAroundAWall`, `TestCheckLosStillSamplesItsDestination`, `TestCheckLosStillBlocksAndStillIgnoresTheStartCell` -- because they build blockers with `flags.BlockWalk = true` and so encode the current rule. **What remains open is a DESIGN question, not a defect: should a hunter see over a low wall, water or a ledge (the 9,058 walk-only subtiles)?** That is difficulty, a standing constraint, so it is Josh's -- and whichever rule ships must ship WITH a dial pass, because `Chance` 0.35, `MaxGroups` 8 and `Radius` 12 were all tuned against the stricter rule. **SHIPPED AS A NAMED DIAL, AND THE DECISION WAS MADE BY MEASURING BOTH WORLDS RATHER THAN BY READING THE FLAG NAMES.** `checkLos` now asks `MapEngine.sightBlocked(flags)` and an unexported `sightRule` field names three rules (unexported deliberately: an exported setter whose only callers are its own tests is the hollow-symbol class the reachability register exists to find, so re-running the A/B means changing the one line in `CreateMapEngine`, and the shipped rule is pinned by a CI test instead): **`SightBlockedBySightFlag` (BlockLOS alone) ships and is the zero value**, `SightBlockedByEither` (the union -- it reads the authored bit and, because BlockLOS is a subset here, is behaviourally the pre-fork rule on this art), and `SightBlockedByWalkFlag` (the pre-19-September rule every number in the record was measured under). The zero value is the shipped rule deliberately: the package's tests build `&MapEngine{}`, and a first cut with another rule at zero had two tests pinning the helper instead of the engine. **THE MEASUREMENT THAT DECIDED IT**, one full cycle each, shipped dials, same seed, nothing spawned and nothing watched: **walls as cover -> 1 encounter, peak hunters aware 0-1, health 240 -> 232** (and at the old `MaxGroups` 8 the record's own six-night run called that night *"one squall at dusk then a long empty walk"*); **darkness as cover -> 12 encounters, peak aware 5, health 240 -> 13, survived.** The first is not a harder game, it is an ABSENT one: on D2's art the town's clutter means an 8-12 tile ray is almost never clear, which is the build Josh launched and could do nothing in. **And it is what the design asks for anyway** -- the notice radius is 12 tiles and DOUBLES for a lit target, R2 makes light a combat system, the slice is *"the night is the enemy"*: cover here is darkness and distance, not geometry. Walls-as-cover at 26.7% density was an accident of inherited art. **The cost is stated rather than buried:** water, low fences, carts and ledges no longer stop the eye, and on these DT1s neither does most of what should. A cover census over 32 bearings at nine radii puts the remaining cover at **1-3 of 32 bearings inside the notice radius (3-9%), rising to 9 of 32 at 24 tiles** -- sparse but real, and `playtest/spawns_test.go`'s act 1 went red until its ring sweep moved from 8 bearings to 32, because 8 is a coin toss at that density. **Pathfinding is unaffected:** the A* reads `flags.BlockWalk` directly (`astar.go:117`), and `checkLos` feeds only sight and the harness's straight-line control, so no route smooths itself through a wall. **The dial pass that had to come with it moved `MaxGroups` 8 -> 2** (see BUG-11: the first sweep was void, the second is the one in the code). **Tests: `blockAt` now sets BOTH bits** (a wall stops walking and sight) and three cases the corpus never had -- `TestCheckLosNoLongerStopsAtAWalkOnlyBlocker` (and both alternative rules exercised in the same test), `TestCheckLosHonoursASightOnlyBlocker` with the pre-fork rule as its negative control, and `TestSightShipsTheSightFlagAndTheZeroValueIsThatRule`, a nine-case table over all three rules. Full evidence: `The Sight Burst - S0 Measurements.md` **and its addendum, `The Sight Burst - S0 Measurements, ADDENDUM AND CORRECTIONS` -- the afternoon document's §3 and §4 are superseded by the addendum's §6, and the correction is part of the evidence** |
| BUG-10 | 2026-09-19 | `34916d33` | **EIGHTEEN LIVE MONSTERS STAND INSIDE THE NOTICE RADIUS AND NOT ONE IS AWARE.** Measured at 22:14 on seed 1462 with **no dial touched, nothing spawned and nothing watched** (scaffold kept at `strigoi-harness-runs\zz_emptyhours_test.go.keep`): 41 NPCs alive on the map, **23 inside the shipped 12-tile radius — 18 of them LIVE** (animations `WL` and `NU`) at 2.7 / 3.8 / 4.8 / 7.2 / 7.8 / 7.8 / 8.3 tiles — **and `notice_aware` is EMPTY.** The five at 1.4 tiles are corpses (`DD`) and are correctly unwatched; that part is the resolver working. **It is not geometry** (they are well inside the radius), **and it is not the raycast at that range**: a second scaffold (`zz_sensor_test.go.keep`) measured the player's own tile as **25/25 walkable** and `straight_line_clear=true` for rays at (+1,+1), (-1,-1), (+1,0), (0,+1) and (+2,+2), in both directions. So `Notice.evaluate`'s `sees = distance <= reach && sight.Clear(...)` should be true for all eighteen. **This is the mechanism behind the record's "six empty hours", zero routs in six nights and quick-resolve never firing** — the night is front-loaded because everything that survives dusk goes permanently blind. **ONE CONFIRMED MECHANISM, found by reading and not yet proven to be the whole of it: `combat_resolver.go:930` withdraws a ROUTED enemy — `e.routed[id] = true; c.withdraw(id)` — and a routed monster is ALIVE.** `withdraw` is `Release` + `Unwatch`, and **nothing ever re-watches**: watches are created when the spawn tables place a group, so an unwatched survivor can never notice the player again for the rest of the run. The other two `withdraw` callers (`:407` quick-resolve, `:814` a death) are deaths and are correct. **What is NOT established: whether rout accounts for all eighteen** (the 19 Sep run reported no rout), or whether a second path drops or never creates their watches. **The measurement to run first:** dump `Notice.Count` / `Checks` against the live-NPC count at 22:00, and `group_list`'s per-group `aware`/`notice`, to separate "has a watch and evaluates false" from "has no watch at all". Until that is known the fix is unknown | **18 of 18 live monsters inside the radius, 0 aware** (one seed, one 25-hour run, stationary player) | **the sight burst took the sight half and BUG-11 took the cap half; the remaining half -- what a routed monster DOES -- is unassigned** and needs one behaviour decision, below | **HALF FIXED 19 Sep 2026, and the hypothesis in the "rate" column was right about the mechanism and wrong about the cause of the eighteen.** Two distinct things were making the night go quiet and they were being read as one. **(a) SIGHT, and this was most of the eighteen:** `checkLos` read the walk flag, so at 8-12 tiles `sight.Clear` refused rays it should have passed -- fixed as BUG-9, and **measured on the unaided chain afterwards: first awareness 19:56 -> 03:00, peak hunters aware 3 -> 5, encounters 5 -> 12.** Note what this does NOT say: BUG-10's own sensor probe had already measured short rays (1-2 tiles) as clear under the OLD rule, so sight was never the reason a monster 2.7 tiles away was blind. Sight explains the 8-12 tile blindness; it does not explain the close ones, and conflating them is what made the row's estimate of "rout accounts for all eighteen" look plausible. **(b) THE GROUP CAP, which was the rest of the quiet:** the cap counted beaten packs, so once two packs were dead the tables never fired again -- fixed as BUG-11. **What remains, and it is confirmed rather than hypothesised: `combat_resolver.go` withdraws a ROUTED enemy, and a routed monster is ALIVE.** `withdraw` is `Release` + `Unwatch`, nothing ever re-watches, and nothing makes it flee either -- so a routed beast stands exactly where it broke, for the rest of the run, blind and inert. That is not a design; it is an absence, and it has more than one correct answer, which is why it is not fixed here: **(i) they flee and leave** -- a retreat route and a despawn once they are far away, which needs a retreat mode `Pursuit` does not have; **(ii) their nerve comes back** -- morale climbs after N world minutes and their members are re-watched, which is cheap (the third provider rule again: `Hurt` is the decrement and there is no increment) and is what R2's own Battle Brothers reference does. **RECOMMENDED (ii)**, with the recovery window a `[DIAL]` measured against the probe, because it uses machinery that exists, it gives the night a second wave from a pack the player already met, and R2 signs "wounds heal through time and resources" for the player's side of the same arithmetic. **Not yet measured: whether a routed pack accounts for ALL the remaining inert survivors** now that (a) and (b) are out of the way; the 19 Sep run reported no rout at all, so a re-run of the 22:00 census is the first thing the next burst should do rather than assuming rout is the whole of it |
| BUG-11 | 2026-09-19 | `f3d63f6b` (and every build since M4.3b) | **THE GROUP CAP COUNTED CORPSES, so a night went permanently quiet once the player had won.** `Spawns.check` refused a spawn on `len(s.groups) >= MaxGroups`, and `s.groups` holds every group the tables have ever made and not yet sent home. Nothing retires a pack the player BEAT: `Despawn` has exactly two callers, the harness and `clearAtDaybreak`, and daybreak is `StageDay` only and skips any group that has noticed the player. So a pack whose members were all killed or all routed sat in the map holding a cap slot until sunrise. **This is the SAME defect `clearAtDaybreak` was written for on 28 Aug, in a second costume.** That one was found by the reachability register and was visible because it was permanent -- the eighth pack was the last the game would ever make. This one is bounded by the night, so it presents as pacing: the night front-loads, goes silent, and every instrument stays green. It became lethal the moment the cap moved 8 -> 2 on the same day's dial pass, where two dusk packs are reached in the first hour of dark. **Found by `TestCombatRout`**, which failed reporting two groups at `morale:0 routing:true notice:[]` after forty five-minute steps produced no arrival at all -- the empty `notice` block is the tell, because `Combat.withdraw` (`Release` + `Unwatch`) is the only thing that unwatches, and it runs on a death and on a rout | **measured: 2 live groups, 0 arrivals over 200 world minutes at a forced chance of 100** -- and at shipped dials it is every night after the second pack falls | **the sight burst**, because it was found inside it and because the dial the burst had just signed is the thing that made it bite | **FIXED 19 Sep 2026.** The cap now counts `liveGroups()`, and a group is `spent` when nothing is watching any member of it -- derived from the mechanism (a member leaves the notice model only by dying or breaking) rather than from a symptom like morale, which a wiped-out pack can reach without ever routing. **The spent group is NOT despawned**, deliberately: `Spawns.Despawn` calls `spawner.Despawn`, which removes the entity AND releases its body, and the bodies of the pack's dead are the carrion the tables feed on. So they stay on the books, out of the cap, until daybreak sends them home. `groups`, `live_groups` and `spent_groups` are all three reported. **`max_groups` became settable in the same change** -- the third provider rule, paid late: it was reported from the day it existed with no verb to move it, so the sweep that tunes it had to edit the source and rebuild between runs. **Three negative controls, each broken in place and restored from a copy kept outside the repo:** the cap back to `len(s.groups)` -> both new tests red on the "freed slot" assertion · `spent` ignoring the `watching` return -> the CONTROL arm red ("the cap holds while every group is live"), which is what proves the test is not vacuous · `liveGroups` losing its nil-notice short-circuit -> the nil arm red. **A fourth control found a bug in the fix itself:** `spent` also guarded `s.notice == nil`, the control written to prove that guard load-bearing PASSED with the guard inverted, and the guard turned out to be unreachable because `liveGroups` answers "cannot tell" before it ever asks -- one condition, one place, and the dead copy is gone. **Two tests, and the second is the one that would have caught this:** `TestSpawnsTheCapCountsLiveGroupsNotSpentOnes` is the unit half, and `TestSpawnsAPackKilledThroughTheResolverFreesItsSlot` wires a real `Spawns` to a real `Combat` through the two interfaces the game screen wires them with -- `Profiles` and `Morale` are both `*Spawns` -- over one real `Notice`, and kills the pack through the resolver. **No test in this package had ever joined those two systems**, which is the same composition gap the whole 19 September audit is about |

## Launch ledger

One line per playtest / harness run: **date · build · launches · failures · note.**
Seeded from §0(a) and the counts already in the record. Every playtest launch from
the hardening burst on is added here.

| date | build | launches | failures | note |
|---|---|---|---|---|
| 2026-09-09 | winnability run | 7 | 0 | 7/7 clean (seed×class matrix; `Winnability Run - 9 Sep 2026.md`) |
| 2026-09-10 | 10 Sep suite | ≥15 | (unrecorded) | the playtest suite (`Session - Claude Code 10 Sep 2026.md`) |
| 2026-09-11 | M4.4a | 4 | (unrecorded) | M4.4a build note |
| 2026-09-14 | `344da610` (pre-edits) | 1 | 0 | §0(a) escape-pause act; clean launch, no map-load crash (`game-20260914-203931.log`) |
| 2026-09-14 | hardening burst (harness) | 17 | 0 | full playtest suite via `scripts\playtest.ps1`, all 24 tests green, no map-load crash (`playtest-hardening-2026-09-14.txt`) |
| 2026-09-15 | M4.4c-1 (overnight, Claude Code) | (unrecorded) | 0 | the build's own closeout run: 25 tests green including `TestSquadsOnScreen`, per the session transcript; the launch count was not recorded before the session hit its usage limit |
| 2026-09-15 | M4.4c-1 review repairs (Cowork) | 31 | 0 | 1 squads act + 4 squads/resolver + 8 negative-control runs + the 18-launch full suite. **Seven of those runs went red on purpose** (the negative controls); **no map-load crash in any of the 31** (`playtest-c1-2026-09-15.txt`) |
| 2026-09-17 | the sight burst (BUG-8) | 20 | 0 | full playtest suite via `scripts\playtest.ps1 -Run .`, **25/25 green, 0 FAIL, 0 panic**, no map-load crash in any of the 20 (`playtest.txt`, `game-20260917-19*.log`). Gate green at the same tree (`gate.txt`: ok=39 fail=0). Three negative controls were run and restored before the commit |
| 2026-09-18 | M4.4c-2a first half (Cowork) | 1 | 0 | `TestNightAndLight` re-run alone to prove the burnt-out torch's removal (`Light.Remove`'s first game caller) leaves `night_light_test.go:151`'s post-burn-out read standing: PASS in 8.97 s, no map-load crash. **The full 17-script suite is owed at c-2a's closeout, not here** — nothing else in this half of the burst launched the harness. |
| 2026-09-19 | M4.4c-2a second half (Claude Code) | ~44 | 0 | c-2a's closeout: the full **17-script suite green** (`go test -tags playtest ./playtest/...` → `ok … 235 s`, `/tmp/playtest-all.txt`), plus ~15 development launches writing `hands_test.go`, the 7 negative-control runs, and `TestS0Instrument` (the ring probe). **No map-load crash in any of the ~44.** The 7 negative-control runs and several development runs went red on purpose (test/geometry fixes and the intentional breaks); none was a crash. Gate green at the same tree (39 ok / 0 fail); reach gate 111, EXIT=0. |

*The night is the enemy · history is the clock · grounded, then supernatural · one region done deeply.*
