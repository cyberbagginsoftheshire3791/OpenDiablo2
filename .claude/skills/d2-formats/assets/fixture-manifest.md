# Fixture manifest — pointer

The canonical fixture manifest for this repository lives at
**`docs/fixtures-manifest.md`** (the rule, the list of every tracked fixture
with origin and justification, the removed-fixture history, and the
`git ls-files` compliance check). This file exists only because the skill
scaffold expected one here; it is deliberately not a copy. Docs point, they
don't mirror.
